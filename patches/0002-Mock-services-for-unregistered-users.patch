From c39efca1ad12d80f26ab28f8ad13eeed01ad96a8 Mon Sep 17 00:00:00 2001
From: Undefined01 <amoscr@163.com>
Date: Fri, 17 Apr 2026 14:09:29 +0800
Subject: [PATCH 2/2] Mock services for unregistered users

---
 src/plus/gk/organizationService.ts |  7 +++
 src/plus/gk/subscriptionService.ts | 94 +++++++++++++++++++++---------
 2 files changed, 74 insertions(+), 27 deletions(-)

diff --git a/src/plus/gk/organizationService.ts b/src/plus/gk/organizationService.ts
index b90518d1c..93a272ebb 100644
--- a/src/plus/gk/organizationService.ts
+++ b/src/plus/gk/organizationService.ts
@@ -64,6 +64,13 @@ export class OrganizationService implements Disposable {
 				if (this._organizations != null) return this._organizations;
 			}
 
+			const token =
+				options?.accessToken ?? (await this.container.subscription.getAuthenticationSession(true))?.accessToken;
+			if (token === 'Unregistered') {
+				this.updateOrganizations([]);
+				return this._organizations;
+			}
+
 			let rsp;
 			try {
 				rsp = await this.connection.fetchGkApi(
diff --git a/src/plus/gk/subscriptionService.ts b/src/plus/gk/subscriptionService.ts
index 271233229..79e7702c2 100644
--- a/src/plus/gk/subscriptionService.ts
+++ b/src/plus/gk/subscriptionService.ts
@@ -1021,34 +1021,73 @@ export class SubscriptionService implements Disposable {
 		this._lastValidatedDate = undefined;
 
 		try {
-			const checkInData = {
-				id: session.account.id,
-				platform: getPlatform(),
-				gitlensVersion: this.container.version,
-				machineId: env.machineId,
-				sessionId: env.sessionId,
-				vscodeEdition: env.appName,
-				vscodeHost: env.appHost,
-				vscodeVersion: codeVersion,
-			};
+			let data: GKCheckInResponse;
+			if (session.accessToken === 'Unregistered') {
+				const now = Date.now();
+				const mockDate = createFromDateDelta(new Date(now), { days: -5 });
+				data = {
+					user: {
+						id: 'Unregistered',
+						name: 'Unregistered User',
+						email: 'unregistered@example.com',
+						status: 'activated',
+						createdDate: mockDate.toISOString(),
+					},
+					licenses: {
+						paidLicenses: {
+							'gitlens-pro': {
+								latestStatus: 'active',
+								latestStartDate: mockDate.toISOString(),
+								latestEndDate: createFromDateDelta(mockDate, { years: 10 }).toISOString(),
+								organizationId: organizationId,
+							},
+						},
+						effectiveLicenses: {
+							'gitlens-pro': {
+								latestStatus: 'active',
+								latestStartDate: mockDate.toISOString(),
+								latestEndDate: createFromDateDelta(mockDate, { years: 10 }).toISOString(),
+								organizationId: organizationId,
+							},
+						},
+					},
+				};
+			} else {
+				const checkInData = {
+					id: session.account.id,
+					platform: getPlatform(),
+					gitlensVersion: this.container.version,
+					machineId: env.machineId,
+					sessionId: env.sessionId,
+					vscodeEdition: env.appName,
+					vscodeHost: env.appHost,
+					vscodeVersion: codeVersion,
+				};
 
-			const rsp = await this.connection.fetchGkApi(
-				'gitlens/checkin',
-				{
-					method: 'POST',
-					body: JSON.stringify(checkInData),
-				},
-				{ token: session.accessToken, organizationId: organizationId },
-			);
+				const rsp = await this.connection.fetchGkApi(
+					'gitlens/checkin',
+					{
+						method: 'POST',
+						body: JSON.stringify(checkInData),
+					},
+					{ token: session.accessToken, organizationId: organizationId },
+				);
 
-			if (!rsp.ok) {
-				this._getCheckInData = () => Promise.resolve(undefined);
-				throw new AccountValidationError('Unable to validate account', undefined, rsp.status, rsp.statusText);
+				if (!rsp.ok) {
+					this._getCheckInData = () => Promise.resolve(undefined);
+					throw new AccountValidationError(
+						'Unable to validate account',
+						undefined,
+						rsp.status,
+						rsp.statusText,
+					);
+				}
+
+				data = await rsp.json();
 			}
 
 			this._onDidCheckIn.fire({ force: force });
 
-			const data: GKCheckInResponse = await rsp.json();
 			this._getCheckInData = () => Promise.resolve(data);
 			this.storeCheckInData(data);
 
@@ -1235,11 +1274,12 @@ export class SubscriptionService implements Disposable {
 			scope?.error(ex);
 		}
 
-		if (session == null) {
-			scope?.addExitInfo('No valid session was found');
-			await this.logoutCore(source);
-			return session ?? null;
-		}
+		session ??= {
+			id: 'Unregistered',
+			accessToken: 'Unregistered',
+			account: { label: 'Unregistered', id: 'Unregistered' },
+			scopes: authenticationProviderScopes,
+		};
 
 		try {
 			await this.checkInAndValidate(session, source, { showSlowProgress: createIfNeeded, force: createIfNeeded });
-- 
2.53.0

